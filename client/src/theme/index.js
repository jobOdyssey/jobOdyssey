import {DefaultTheme} from 'react-native-paper';

export const AppTheme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#aed581',
    accent: '#e1ffb1',
  },
};